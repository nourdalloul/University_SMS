1. Encountered SSL/certificate (OpenSSL) issues during Docker installation on the new Linux environment, which required troubleshooting certificate trust and verification errors before proceeding.

2. Needed to coordinate with the network administrator to request external firewall access for the service port so the deployed service could be reached from outside the network.